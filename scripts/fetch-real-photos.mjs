/**
 * DISABLED — AURA uses locally generated photography only (no Unsplash).
 * Use: npm run deploy:photos
 */
console.error(
  "fetch-real-photos.mjs is disabled. Use `npm run deploy:photos` for local assets.",
);
process.exit(1);
